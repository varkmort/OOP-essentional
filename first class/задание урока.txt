class Duration{
-seconds:float;
+setDuration(time:float):void;
+getDurtion():float;
+addTime(time:float):void;
};

методы которые сообщают количество минут и часов хранящихся в объекте

метод который добавляет значение одной длительности к другой add(Duration)

дружественную функцию которая возвращает разницу между двумя длительностями времени